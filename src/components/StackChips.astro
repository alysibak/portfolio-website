---
interface Props {
  stack: string[];
}

const { stack } = Astro.props;
---

<ul class="flex flex-wrap gap-1.5" aria-label="Stack">
  {
    stack.map((tech) => (
      <li class="meta rounded-full border border-border px-2.5 py-1 text-[0.75rem] text-muted">{tech}</li>
    ))
  }
</ul>
