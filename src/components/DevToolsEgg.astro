<script is:inline>
  (function () {
    console.log("you opened the console. of course you did.");
    console.log("everything on this site is real, including this.");
    console.log(
      "the bugs in /work are real bugs i found. the terminal is wired, not faked."
    );
    console.log("press / or ~ anywhere on the site to open the shell. try: help");
    console.log(
      "if you're reading this, we'd probably get along: asibak@uoguelph.ca"
    );
  })();
</script>
